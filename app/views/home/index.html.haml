.row
  .col-md-8
    %h1= "hello #{user_signed_in? ? current_user.email : 'world'}"

    = form_tag({ controller: "locations", action: "search" }, method: "get") do
      = text_field_tag(:query)
      = submit_tag("Search")

    .map-hero
      #map
      
  .col-md-4
    - if user_signed_in?
      %h4 My Groups
      %ul
        - current_user.groups.each do |group|
          %li= link_to group.name, group_path(group)
